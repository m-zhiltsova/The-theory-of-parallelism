while (eps > 0.001 && iter < 10000) {
    for (i = 0; i < n; i++) {
        double sum = 0.0;
        for (j = 0; j < n; j++)
            sum += a[i][j] * xo[j];
        x[i] = xo[i] - tau * (sum - b[i]);
    }
    eps = fabs(x[0] - xo[0]);
    for (i = 0; i < n; i++) {
        if (fabs(x[i] - xo[i]) > eps)
            eps = fabs(x[i] - xo[i]);
        xo[i] = x[i];
    }
    iter++;
}

while (eps>0.001) {
    for(i=0;i<n;i++) {
	    x[i]=0;
	    for(j=0;j<n;j++) 
			x[i]=x[i] + a[i][j]*xo[j];
	    x[i]=x0[i] - tau * (x[i] - b[i]);
	}
	eps = fabs(x[0] - xo[0]);
	for(i = 0; i < n; i++) {
	    if(fabs(x[i] - xo[i]) > eps)
	    	eps = fabs(x[i] - xo[i]);
	    xo[i]=x[i];
	}
}
